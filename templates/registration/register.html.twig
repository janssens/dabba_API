{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
</section>

    {% for flashError in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}

    <div class="w-full sm:h-screen flex justify-center items-center bg-image bg-no-repeat bg-cover bg-center" style="--bg-image:url('{{ asset('food-photographer-david-fedulov-FtpgFeUQuAY-unsplash.jpg') }}');">
        <div class="p-10 bg-white w-full sm:w-auto rounded flex justify-center items-center flex-col shadow-md">
            <h1>Créer un compte</h1>

            {{ form_start(registrationForm) }}
                {{ form_row(registrationForm.firstname, {
                    label: 'Mon prénom'
                }) }}
                {{ form_row(registrationForm.lastname, {
                    label: 'Mon nom'
                }) }}
                {{ form_row(registrationForm.email, {
                    label: 'E-mail'
                }) }}
                {{ form_row(registrationForm.plainPassword, {
                    label: 'Mot de passe'
                }) }}
                {{ form_row(registrationForm.agreeTerms, {
                    label: 'Je valide les conditions générales'
                }) }}
                <button class="bg-purple-600 hover:bg-purple-900 text-white font-bold p-2 rounded w-80" type="submit"><span>Créer mon compte</span></button>
            {{ form_end(registrationForm) }}
        </div>
    </div>

    <section>
{% endblock %}
